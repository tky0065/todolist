import {describe, expect,it} from '@jest/globals';

describe('#',()=>{
    it('Should have an error message', () => {
        // given
        //when
        //then
        expect(true).toBe(true);
    })
})